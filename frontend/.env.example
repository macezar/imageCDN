# Frontend Environment Variables

# API Base URL (optional)
# In development: Leave empty to use Vite proxy (/api → http://localhost:5000)
# In production: Leave empty to use same origin (/api)
# For custom backend URL: Set to full URL (e.g., https://api.example.com)
# VITE_API_URL=

# Example configurations:
# VITE_API_URL=/api                          # Use relative URL (default)
# VITE_API_URL=http://localhost:5000/api    # Direct to backend (bypasses proxy)
# VITE_API_URL=https://api.example.com/api  # Production API server
